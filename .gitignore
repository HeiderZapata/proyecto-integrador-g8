# --- Datos crudos y pesados (el repo NO es un data lake) ---
/data/
*.csv
*.parquet
*.tsv
# Excepción: la Gold AGREGADA pequeña para Power BI SÍ se versiona (doc 00 §12.2).
!reports/data/*.csv
!reports/data/*.parquet

# --- Material del curso (peso + IP del profe) ---
material_cursos/

# --- Secretos y credenciales (NUNCA al repo) ---
.env
*.env
kaggle.json
*secret*
*credential*
*.key

# --- Checkpoints de streaming / Spark ---
checkpoint/
_checkpoints/
spark-warehouse/

# --- Power BI: decidir en equipo si versionar el .pbix (es binario y pesado) ---
# *.pbix

# --- Andamiaje local del refactor del EDA (cell sources + backups, no es entregable) ---
notebooks/analysis/_build/

# --- Basura del sistema y editores ---
.DS_Store
.ipynb_checkpoints/
__pycache__/
.vscode/
.idea/

# Office lock/temp files
~$*
